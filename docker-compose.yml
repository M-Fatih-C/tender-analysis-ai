services:
  tenderai:
    build: .
    container_name: tenderai
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./.env:/app/.env:ro
    environment:
      - DEMO_MODE=${DEMO_MODE:-false}
    restart: unless-stopped
